  function loadFile_b(event) {
    var output = document.getElementById('wst_bc_1');
    output.src = URL.createObjectURL(event.target.files[0]);
  };
    function loadFile_l(event) {
    var output = document.getElementById('wst_bc_2');
    output.src = URL.createObjectURL(event.target.files[0]);
  };
    function loadFile_f(event) {
    var output = document.getElementById('wst_bc_3');
    output.src = URL.createObjectURL(event.target.files[0]);
  };
    function loadFile_bc(event) {
    var output = document.getElementById('wst_bc_1c');
    output.src = URL.createObjectURL(event.target.files[0]);
  };
    function loadFile_lc(event) {
    var output = document.getElementById('wst_bc_2c');
    output.src = URL.createObjectURL(event.target.files[0]);
  };
  function loadFile_lca(event) {
    var output = document.getElementById('wst_bc_2ca');
    output.src = URL.createObjectURL(event.target.files[0]);
  };

   function loadFile_lcasldr(event) {
    var output = document.getElementById('wst_bc_sld');
    output.src = URL.createObjectURL(event.target.files[0]);
  };

  